{
  "name": "runeterra-reclaimed",
  "version": "0.57.0",
  "private": true,
  "description": "A League of Legends-themed incremental game modelled on Kittens Game. The game is index.html; everything else is apparatus.",
  "type": "module",
  "scripts": {
    "syntax": "node -e \"const fs=require('fs');const m=fs.readFileSync('index.html','utf8').match(/<script>([\\s\\S]*)<\\/script>/);new Function(m[1]);console.log('syntax OK')\""
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
